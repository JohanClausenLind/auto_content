# `assets/music` — non-verbal background music for video

22 instrumental tracks (14:36 of audio) generated locally with
**MiniMax-Music3-GGUF** through the HOT-Step engine. 44.1 kHz stereo, 24-bit FLAC.
Nothing here was downloaded; nothing here was uploaded.

**Do not hand-edit this directory.** It is generated by
`skills/audio/music/build_library.py` from `skills/audio/music/library.json`; every track
has a fixed seed, so the set is reproducible. `manifest.json` carries per-file sha256,
loudness and QC numbers. The design decisions are in
`docs/research/2026-09-07-background-music-library.md`.

## All of it is underscore

Every track is **fully instrumental** — no vocals, no vocal samples — and written to sit
*under* narration rather than beside it: slow simple harmony, short motifs instead of
melodies, texture instead of arrangement, and no drum kit anywhere. That is the brief, not
a limitation: a hummable tune competes with a speaking voice for the same attention.

## Levels and leaving room for the voice

Every track is levelled to integrated **-23.0 LUFS**, ceiling -1.0 dBTP,
with a single constant gain — no compression, no limiting. That sits well under a -14 LUFS
programme, and under the -20..-30 dB that music wants to be at while somebody is speaking.

`qc.presence_band_ratio` is the number to read before choosing a bed: the share of the
track's energy between 1 kHz and 4 kHz, which is the band a narrator competes for. Lower is
safer under dialogue. **The library is deliberately not EQ-carved** — carving bakes in an
assumption about a particular voice, and the pipeline already ducks music under speech
(sidechain in `add_music_bed`). The measurement is here so you can pick a bed that needs
less of it.

## Does it move?

`loudness.loudness_range_lu` (EBU R128 LRA) says whether a track holds one level or travels.
Each entry declares the band it was written for, and QC flags a track that missed it —
`too_flat` for an arc that never arrived, `too_dynamic` for a bed that jumps around under an
edit. Textures sit near 0-3 LU; the arc tracks are the high end.

## Looping

Only the `texture` tracks are marked `loopable`, and only they are safe to butt-join: they
are pad and drone material with no phrase structure, so the same tail-over-head wrap the
sfx beds use applies cleanly. **The other tracks are not loops** — music has phrases, and
wrapping one mid-phrase is audible however well the crossfade is done. Use those at their
written length, or cut them on a musical boundary.

## documentary

| id | file | length | LRA | 1-4 kHz | use |
|---|---|---|---|---|---|
| `doc_hopeful_resolve` | `documentary/doc_hopeful_resolve.flac` | 0:37 | 5.8 dB | 0.13 | Closing sequences and end cards. Resolves without becoming triumphant. |
| `doc_piano_felt` | `documentary/doc_piano_felt.flac` | 0:42 | 17.5 dB | 0.02 | Intimate close-up interviews and quiet personal moments. |
| `doc_solemn_low` | `documentary/doc_solemn_low.flac` | 0:45 | 10.3 dB | 0.12 | Grave subject matter, archive footage, historical segments. |
| `doc_strings_piano_arc` | `documentary/doc_strings_piano_arc.flac` | 0:44 | 4.8 dB | 0.12 | A segment that needs to move: gathers pace in the middle, then settles again. The 'quicker then slower' bed. |
| `doc_strings_reflective` | `documentary/doc_strings_reflective.flac` | 0:32 | 6.0 dB | 0.23 | The default documentary bed. Reflective interview and observational footage. |

## explainer

| id | file | length | LRA | 1-4 kHz | use |
|---|---|---|---|---|---|
| `corporate_uplift_soft` | `explainer/corporate_uplift_soft.flac` | 0:32 | 10.8 dB | 0.20 | Company and promotional pieces that need warmth without a hard sell. |
| `data_calm_pulse` | `explainer/data_calm_pulse.flac` | 1:12 | 6.3 dB | 0.11 | Charts, figures and data segments where the narration carries the detail. |
| `explainer_light_pulse` | `explainer/explainer_light_pulse.flac` | 0:43 | 5.4 dB | 0.07 | General explainer and how-to narration. Light, uncomplicated, non-distracting. |
| `explainer_neutral_marimba` | `explainer/explainer_neutral_marimba.flac` | 1:17 | 6.9 dB | 0.03 | The most neutral bed here. Use when the content, not the mood, should carry. |
| `tech_minimal_grid` | `explainer/tech_minimal_grid.flac` | 0:33 | 8.3 dB | 0.12 | Product, software and technical demos. Clean and modern without being cold. |

## human

| id | file | length | LRA | 1-4 kHz | use |
|---|---|---|---|---|---|
| `human_bittersweet_strings` | `human/human_bittersweet_strings.flac` | 0:51 | 8.6 dB | 0.22 | Loss, memory and reflection. Sad without being sentimental. |
| `human_warm_piano` | `human/human_warm_piano.flac` | 0:37 | 10.8 dB | 0.12 | Personal stories and human-interest segments. |

## nature

| id | file | length | LRA | 1-4 kHz | use |
|---|---|---|---|---|---|
| `nature_gentle_curious` | `nature/nature_gentle_curious.flac` | 1:16 | 4.8 dB | 0.30 | Wildlife and small-scale nature: something being examined up close. |
| `nature_open_wide` | `nature/nature_open_wide.flac` | 0:32 | 13.7 dB | 0.09 | Aerials, landscapes, wide establishing shots. |
| `nature_vast_slow` | `nature/nature_vast_slow.flac` | 0:17 | 4.4 dB | 0.13 | Very slow wide shots: ice, ocean, desert, deep time. |

## tension

| id | file | length | LRA | 1-4 kHz | use |
|---|---|---|---|---|---|
| `doc_investigative_pulse` | `tension/doc_investigative_pulse.flac` | 0:25 | 6.1 dB | 0.22 | Procedural or investigative narration that needs forward motion without drama. |
| `doc_tension_slow_build` | `tension/doc_tension_slow_build.flac` | 1:22 | 6.2 dB | 0.15 | Soft suspense under a science explainer or an unfolding investigation. |
| `doc_unease_drone` | `tension/doc_unease_drone.flac` | 0:22 | 4.1 dB | 0.32 | Dread and uncertainty. Almost textural — use where music would be too much. |

## texture

| id | file | length | LRA | 1-4 kHz | use |
|---|---|---|---|---|---|
| `calm_texture_air` | `texture/calm_texture_air.flac` | 0:20 | 0.8 dB | 0.25 | A light airy presence. Sits above the voice rather than under it. Loopable. |
| `calm_texture_deep` | `texture/calm_texture_deep.flac` | 0:09 | 0.0 dB | 0.23 | Low weight and seriousness with almost no musical content. Loopable. |
| `calm_texture_glass` | `texture/calm_texture_glass.flac` | 0:12 | 0.7 dB | 0.34 | Sparse delicate tones for reflective pauses. Loopable. |
| `calm_texture_warm` | `texture/calm_texture_warm.flac` | 0:23 | 2.4 dB | 0.02 | Barely music. A warm presence under narration where a tune would be too much. Loopable. |

