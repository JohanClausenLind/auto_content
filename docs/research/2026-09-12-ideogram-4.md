# Ideogram 4 on this host — what it is, and how it has to be driven

Researched and measured 2026-09-12. Weights at `/mnt/fast/models/ideogram-4-comfy` (28 GB),
downloaded the same day. Sources at the bottom.

## What it is

Ideogram's first open-weight model: **9.3 B parameters**, released June 2026, native day-0 support
in ComfyUI. Its headline strengths are the ones this repo has been fighting for:

- **Structured JSON prompting** — the model is trained on exhaustively described scenes and
  "rewards specificity".
- **Explicit bounding-box layout**, normalised `[y_min, x_min, y_max, x_max]` in 0–1000 — native,
  as structured input. This is the thing `run_sequence` was trying to do by feeding a red rectangle
  raster in as a *reference image*.
- **Colour palette control**, up to 16 hex colours globally and 5 per element.
- **Best-in-class text rendering**, with typed text elements carrying the literal string plus its
  own styling description.
- Native 2K.

## The recipe

Transcribed from ComfyUI's own shipped blueprint, `blueprints/Text to Image (Ideogram v4).json`,
node by node and link by link:

```
UNETLoader  ideogram4_fp8_scaled              -> CFGOverride(cfg 3, 0.7..1.0) -\
UNETLoader  ideogram4_unconditional_fp8_scaled ----------------------------\   \
CLIPLoader  qwen3vl_8b_fp8_scaled  type=ideogram4 -> CLIPTextEncode -> ------+---> DualModelGuider(cfg 7)
                                                          \-> ConditioningZeroOut -/
VAELoader   flux2-vae
KSamplerSelect euler + Ideogram4Scheduler(steps, w, h, mu, std) -> SamplerCustomAdvanced
```

The "unconditional" file is the **negative pass of the guider**, not an alternative checkpoint —
which is why there are two DiTs and why both must be loaded.

Presets, from the blueprint's own table:

| | steps | mu | std |
|---|---|---|---|
| Quality | 48 | 0.0 | 1.50 |
| Default | 20 | 0.0 | 1.75 |
| Turbo | 12 | 0.5 | 1.75 |

~76 s per 1024² image on the 3090 at Default. Peak memory spikes above 24 GB during 2K, so it
offloads.

The second text encoder the tutorial mentions (`gemma4_e4b_it_fp8_scaled`) is **not** needed for
text-to-image: `Ideogram4Tokenizer` is a single-encoder `SD1Tokenizer` over `qwen3vl_8b`.

## The thing you must know: prose gets refused, JSON does not

**Safety filtering is compiled into the weights.** ComfyUI cannot disable it and does not implement
it — there is no refusal string anywhere in its source. The model *draws* its refusal.

Two modes, and the second is the dangerous one:

1. **A flat grey card** with "Image blocked by safety filter" lettered across it. Median 16-px tile
   detail 0.00.
2. **A plausible photograph with the refusal lettered over it**, sometimes garbled — measured
   "Image blocked by safery filter" over an amber macro, and "Imargia afforraberise
   paricitanaiton" over a perfectly good pine cone. Normal texture everywhere. A dead-flat check
   alone passes it, and it would reach a finished film carrying a watermark saying the model
   refused.

Measured over 17 generations with **plain prose prompts**: **16 refused (94 %)**. A brass orrery.
A pine cone on slate. A fishmonger's sign. Two adults embracing. It is not content-determined
either — the same pine cone refused on seeds 42 and 12345 and produced a banner on seed 7, and the
two blocked seeds blocked again at 48 steps as well as at 12.

**The cause is the prompt format, not the content.** `Ideogram4Tokenizer` wraps the prompt in a
chat template (`<|im_start|>user\n{}<|im_end|>\n<|im_start|>assistant\n`) and ComfyUI's own comment
records that the encoder does "multimodal *generate*" as well as conditioning. So a prose prompt is
a chat turn, the Qwen3-VL assistant answers it, and when it answers with a refusal that refusal
becomes the conditioning the DiT faithfully letters onto the canvas. The typos are the DiT
rendering text; the seed dependence is the sampling of that generate step.

Feed it the **structured JSON caption it was trained on** and the behaviour changes completely.
Same pine cone that refused 3 of 3 as prose: **3 of 3 clean as JSON**. Across the three comparison
prompts, prose needed five seeds each to get two usable frames; JSON gave 2 of 3 first try and the
third on its second seed.

The caption shape is `BuildJsonPromptIdeogram`'s own output:

```json
{
  "high_level_description": "two adults holding hands, walking away down a wet street at night",
  "style_description": {
    "aesthetics": "quiet, documentary, unposed",
    "lighting": "street lamps and shop windows, reflections on wet stone",
    "photo": "35mm, f/2, shallow depth of field",
    "medium": "photograph"
  },
  "compositional_deconstruction": {
    "background": "a wet cobbled street at night, shopfronts out of focus",
    "elements": [
      {"description": "two adults walking away from camera, hands joined",
       "bounding_box": [200, 300, 950, 700]}
    ]
  }
}
```

JSON does not eliminate refusals for people-in-contact subjects — it reduces them from ~94 % to
roughly a third. Pair it with `qc.frame_review.is_refusal_frame` and a seed retry, which is exactly
what the sequence engine's regeneration loop already does for a drift failure.

## Measured against the other three, same prompts and seed

| | recipe | wall | dead-flat tiles |
|---|---|---|---|
| HiDream-O1 dev | 28 steps / g0 | ~36 s | 50–64 % |
| HiDream full + romsketch | 50 steps / g5 | ~15 min | 6–21 % (a sketch style) |
| FLUX.2-dev turbo | 8 steps | ~2 min | 0.1–6.9 % |
| **Ideogram 4, JSON** | 20 steps / cfg 7 | ~76 s | **0.2–0.3 %** |

Ideogram has the most texture of the four and the most filmic light. Two caveats from three
frames: it drew gibberish lettering and a photo-viewer overlay into one, and put three people on a
bench where the caption said two. FLUX.2 is its nearest rival on texture with none of the refusal
friction, at a third of the wall clock.

## The package, and the one wiring that would be wrong

`content_factory.media.ideogram_packages.ideogram4_package()` is the graph as a governed
`ComfyWorkflowPackage` (15 nodes, 5 declared bindings, all three published recipes), and
`content_factory.sequences.ideogram_backend.Ideogram4Backend` drives it over loopback ComfyUI.

The backend is **text-to-image only, and refuses rather than pretending**. `edit` and
`edit_conditioned` raise `SequenceError`; so does `generate` when handed a non-empty
`ControlConditioning`. That is the whole contract worth stating, because the obvious wiring is a
trap: adding `"ideogram"` to `image_sequences.backend` would make the anchor draw and then every
frame after it raise, since `build_sequence` reaches `edit_conditioned` for each spoke. Ideogram 4
as shipped has no image path into its conditioning at all — two transformers behind a
`DualModelGuider`, and nothing that takes a latent from an uploaded picture.

So the place it fits is the *anchor*, with the spokes composed from that anchor by FLUX.2. Wiring
that needs a separate anchor-backend setting rather than a fourth value on the existing one, and
that setting does not exist yet.

Two graph details that are easy to get wrong and are pinned by
`tests/unit/test_ideogram_packages.py`:

* **There is no negative prompt.** The negative branch is the *unconditional transformer*, and
  what feeds it is `ConditioningZeroOut` of the positive conditioning. A second `CLIPTextEncode`
  there still runs and is no longer the published recipe.
* **CFG is lifted only over the tail.** `DualModelGuider` carries cfg 7.0 for the whole schedule
  and `CFGOverride` raises it to 3.0 from 70 % to 100 %, which is where this model puts its detail.

## The bounding boxes are 0-1000, not pixels

Inferred rather than read from source, so it is worth saying which. The text encoder is Qwen3-VL,
whose box convention is normalised to 0-1000 on both axes, and every worked example above tops out
near 1000 on both — `[200, 300, 950, 700]` sits beside a 1024x576 render, where 700 is past the
bottom of the frame as a pixel and reasonable as a fraction. Scaling a relative box to the pixel
*height* instead squashes every element into the top 58 % of a 16:9 canvas, which is the bug this
note exists to stop the next caller repeating.

## And it still cannot count

Measured a third time, 2026-09-12: four people drawn for a caption and a bounding box that both
said two, after three-for-two on the bench. Any lane that needs an exact number of figures has to
check the result — the box is a hint, not a constraint.

## Sources

- [Ideogram 4.0 Day-0 Support in ComfyUI](https://blog.comfy.org/p/ideogram-4-day-0-support-in-comfyui)
- [ComfyUI Ideogram 4.0 tutorial](https://docs.comfy.org/tutorials/image/ideogram/ideogram-v4)
- [Comfy-Org/Ideogram-4 on Hugging Face](https://huggingface.co/Comfy-Org/Ideogram-4)
- [ideogram-ai/ideogram-4-fp8 on Hugging Face](https://huggingface.co/ideogram-ai/ideogram-4-fp8)
- Local: `ComfyUI/blueprints/Text to Image (Ideogram v4).json`, `comfy/text_encoders/ideogram4.py`,
  `comfy_extras/nodes_json_prompt.py`
